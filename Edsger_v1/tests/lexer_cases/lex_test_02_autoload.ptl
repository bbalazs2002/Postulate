namespace \Main;

@autoload("\Vendor\Math\", "libs/math/src");
@autoload("\Plugin\:module\", "plugins/:module/src", verified);
@autoload("\Std\", "stdlib/src", unverified);

function main() : int32 [
  requires: true;
  ensures: result == 0;
] {
  return 0;
}
