struct Pair {
  a : int32;
  b : int32;
}

function main() : int32 {
  mut p1: Pair := Pair { a := 1, b := 2 };
  mut p2: Pair := Pair { a := 1, b := 2 };
  if (p1 == p2) {
    return 1;
  } else {
    return 0;
  }
}
