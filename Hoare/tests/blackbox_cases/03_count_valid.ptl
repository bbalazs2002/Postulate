PROGRAM
function count_positive(arr: int32[6]) : int32 {
  mut cnt: int32 := 0;
  mut i: int32 := 0;
  while (i < 6) {
    if (arr[i] > 0) {
      cnt := cnt + 1;
    }
    i := i + 1;
  }
  return cnt;
}
