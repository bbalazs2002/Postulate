PROGRAM
struct Point {
  x : int32;
  y : int32;
}

function find_point_with_x(pts: Point[4], target_x: int32) : int32 {
  mut i: int32 := 0;
  while (i < 4) {
    if (pts[i].x == target_x) {
      return i;
    }
    i := i + 1;
  }
  return -1;
}
