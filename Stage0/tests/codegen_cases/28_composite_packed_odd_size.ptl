struct Small { x: int8; y: int8; }

function bump_small(s: Small) : Small {
  return Small { x := s.x + 1, y := s.y + 1 };
}

function main() : int32 {
  mut r: Small := bump_small(Small { x := 10, y := 20 });
  if (r.x == 11) {
    if (r.y == 21) {
      return 32;
    }
  }
  return 0;
}
